/** Form field name for course image — must match frontend FormData.append('image') */
export const COURSE_IMAGE_FIELD = 'image';

/** Form field name for signup profile image */
export const USER_IMAGE_FIELD = 'image';

/** Max upload size enforced by Multer (5 MB) */
export const MAX_IMAGE_SIZE_BYTES = 5 * 1024 * 1024;
