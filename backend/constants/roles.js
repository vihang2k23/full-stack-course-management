/**
 * User roles used for authorization (admin vs normal user).
 * Keep values in one place so models, validators, and routes stay in sync.
 */

const ROLES = {
  ADMIN: 'admin',
  USER: 'user',
};

export default ROLES;
